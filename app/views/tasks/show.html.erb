<h1>Task #show</h1>
<p>Button for Sending texts via app/views/tasks/show.html.erb</p>

<ul class="tasks">
    <div>
        <%= @task.activity %>
        </br>
        <%= @task.message %>
        </br>
        <%= @task.schedule_time %>
        </br>

        <h3>Being notified via SMS about your goal</h3>

        <% @task.recipients.each do |recipient| %>
          <%= recipient.name %>
          </br>
        <% end %>
    </div>
        

    </br>
    </br>
      <!-- IF this task doesn't have a DJ id, show this button to start a DJ id and instance || this task HAS a DJ id, and delayedjob already has at least one attempt-->
        <div>
          <%= form_for :task, url: {action: 'sms', params: {id: @task.id}} do |f| %>
          
            <%= f.hidden_field :phone, :value => @phones %>
      			<%= f.hidden_field :message, :value => @task.message %>

      		  <p>
      			<%= f.submit "Send Message and Start Timer Now!" %>
            </br>
            </br>
            <%= link_to 'Modify Task Details and Recipients', edit_task_path(@task.id) %>
            </br>
      			<%= link_to 'Go Back', user_path(current_user.id) %>
      		  </p>

    		  <% end %>
        </div>
      <!-- ELSE show delayed_id CREATED AT (when button pressed) and a ticker clock showing what time you can see the button again and REDO the task. -->
</ul>